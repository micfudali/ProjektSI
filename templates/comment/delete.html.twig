{% include 'default/_form.html.twig' with {
    page_title: 'Delete comment'|trans({'%id%': category.id|default('')}),
    form: form,
    submit_label: 'Delete'|trans,
    back_to_list_path: 'post_index',
} %}